coordenadas = (40.71, -74.00, 10) # Arrays que são imutaiveis.
print(f"Suas cordenadas são: {coordenadas}")

estados = ('RJ', 'SP', 'MG')
tupla_nova = estados + ('BA',) # Cria uma tupla nova a partir da antiga.
